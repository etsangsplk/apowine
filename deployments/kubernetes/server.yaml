apiVersion: extensions/v1beta1
kind: ReplicaSet
metadata:
  labels:
      app: apowine-server
      role: backend
  name: apowine-server
  namespace: apowine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: apowine-server
  template:
    metadata:
      labels:
          app: apowine-server
          role: backend
    spec:
      containers:
        - image: aporeto/apowine-server:apostable
          imagePullPolicy: Always
          env:
             - name: APOWINE_MONGOURL
               value: mongodb.apowine.svc.cluster.local:27017
             - name: APOWINE_SERVERPORT
               value: :3000
             - name: APOWINE_MAKENEWCONNECTION
               value: "true"
             - name: APOWINE_USEHEALTH
               value: "true"
             - name: APOWINE_HEALTHPORT
               value: :5001
          name: apowine-server
---
kind: Service
apiVersion: v1
metadata:
  name: server
  namespace: apowine
spec:
  selector:
      app: apowine-server
      role: backend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
---
kind: Service
apiVersion: v1
metadata:
  name: server-count
  namespace: apowine
spec:
  selector:
      app: apowine-server
      role: backend
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
